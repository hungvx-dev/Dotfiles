local colors = require("themes.eryx.colors")

return {
  default = {
    bg = colors.bg,
    fg = colors.fg,
  },
  cursor = {
    bg = colors.bg,
    fg = colors.cyan,
  },
  transparent = {
    bg = colors.bg,
  },
}

local colors = require("themes.feiyu.colors")
return {
  CleverFDefaultLabel = {
    fg = "#ff7788",
    underline = true,
  },
  EyelinerPrimary = {
    fg = colors.cyan, --"#ff1122",
    underline = true,
    bold = true,
  },
  EyelinerSecondary = {
    fg = colors.green,
    underline = true,
  },
}

-- -- # Plugins
-- hl(0, "NeoTreeWinSeparator", { fg = colors.none })
-- hl(0, "NeoTreeDimText", { fg = colors.cyan })
-- --
-- hl(0, "GitSignsCurrentLineBlame", { bg = colors.blue0 })

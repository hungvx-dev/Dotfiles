local colors = require("themes.feiyu.colors")

return {
  default = {
    bg = colors.bg,
    fg = colors.fg,
  },
  cursor = {
    bg = colors.bg,
    fg = colors.cyan,
  },
  transparent = {
    bg = colors.bg,
  },
}
